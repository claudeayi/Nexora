# ============================
# Nexora Futurist — API (Full IA)
# ============================

# ---- App ----
NODE_ENV=development
PORT=4000
JWT_SECRET=change-me-please               # ⚠️ change en prod
CORS_ORIGIN=http://localhost:5173         # Front en dev (Vite)
PUBLIC_URL=http://localhost:8080          # URL Web (prod locale) ou https://yourdomain.com

# ---- Base de données (MySQL) ----
# Local (hors Docker) :
DATABASE_URL=mysql://root:password@localhost:3306/nexora
# Docker Compose (services "db" & "api") :
# DATABASE_URL=mysql://root:password@db:3306/nexora

# ---- Multi-tenant (optionnel) ----
# Clé tenant par défaut utilisée par le seed et le SDK
DEFAULT_TENANT_KEY=default

# ---- IA / Copilote ----
AI_PROVIDER=LOCAL                         # LOCAL (par défaut) | OPENAI (si tu branches un LLM)
AI_URL=http://localhost:8000              # Service FastAPI local (dev)
# En Docker Compose : AI_URL=http://ai:8000

# Si tu passes AI_PROVIDER=OPENAI, alors :
# OPENAI_API_KEY=sk-...

# ---- Paiements ----
# Modes : LOCAL | STRIPE | PAYPAL | CINETPAY
BILLING_MODE=LOCAL

# Stripe (mode test)
STRIPE_SECRET_KEY=
STRIPE_PRICE_PRO=price_xxx                # ex: price_12345

# PayPal (sandbox)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_ENV=sandbox                        # sandbox | live

# CinetPay (sandbox/production selon tes clés)
CINETPAY_API_KEY=
CINETPAY_SITE_ID=

# ---- Logs (optionnel) ----
LOG_LEVEL=info                            # debug | info | warn | error

# ============================
# EXEMPLES PROD (commentés)
# ============================

# NODE_ENV=production
# PORT=4000
# JWT_SECRET=ultra-long-random-string
# CORS_ORIGIN=https://yourdomain.com
# PUBLIC_URL=https://yourdomain.com
# DATABASE_URL=mysql://user:strongpass@your-managed-mysql:3306/nexora
# AI_PROVIDER=LOCAL
# AI_URL=http://ai:8000
# BILLING_MODE=STRIPE
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_PRICE_PRO=price_xxx
