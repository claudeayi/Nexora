# ============================================
# Nexora Copilote Full IA — Frontend (.env)
# ============================================
# ATTENTION : les commentaires doivent utiliser '#' (pas '//').

# URL de l’API backend (Express/Prisma)
VITE_API_URL=http://localhost:4000

# Clé du tenant multi-tenant (header x-tenant)
VITE_TENANT_KEY=default

# --- Options UI (facultatives, sûres par défaut) ---

# Thème par défaut de l’interface: dark | light
VITE_DEFAULT_THEME=dark

# Activer la Marketplace (affichage UI)
VITE_MARKETPLACE_ENABLED=true

# Notifications temps réel:
# - on  : essaye d'abord l'SSE (GET /events/stream), puis fallback polling
# - off : désactive l’SSE (garde uniquement le polling si tu laisses un intervalle >0)
VITE_NOTIFICATIONS_SSE=on

# Intervalle de polling (ms) si l’SSE n’est pas dispo côté backend
VITE_NOTIFICATIONS_POLL_INTERVAL_MS=15000

# (Optionnel) URL WebSocket si tu actives Socket.io côté backend
# VITE_WS_URL=http://localhost:4000

# (Optionnel) Chemin SSE si différent de /events/stream
# VITE_SSE_PATH=/events/stream
